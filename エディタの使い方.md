# KazumimiEngine エディタの使い方ガイド

「どこから触ればいいか」順に並べています。まずは **クイックスタート** の流れで1本作ってみるのがおすすめです。

---

## 0. 基本操作

| キー / 操作 | 効果 |
|---|---|
| **F1** | エディタの表示 / 非表示 |
| **F2** | ドックUI ⇔ アイコンUI の切替 |
| **Ctrl+S** | マップを上書き保存 |
| **Ctrl+Z / Ctrl+Y** | 元に戻す / やり直す（レール・コイン・ブロック） |
| **右クリック（ゲームビュー）** | その場に配置メニュー（後述） |
| メニューバー「ウィンドウ」 | パネル表示・ワークスペース・UI設定（どちらのUIモードでも使える） |

- **アイコンモード**：左のアイコンでパネルを開閉。「作業」の5ボタン（設置/調整/線路/敵/全閉）で一式切替
- **ドロワー**（パネルが縦に並ぶ帯）：縁をドラッグで幅変更、UI設定で左右入替
- 起動時からエディタを開きたい場合：⚙設定 →「起動時にエディタを表示」

---

## 1. クイックスタート：ステージを1本作る流れ

1. **線路**（レールエディタ）→ 作成タブ → 「形」から直線を生成 → ゲームビューでクリック設置
2. **形を整える** → 作成タブの **2D形状エディタ** で点をドラッグ（後述）
3. **配置** → ゲームビューで**右クリック** → 敵 / コイン / ブロックをその場に配置
4. **確認** → 右クリック → **「ここからテストプレイ」**（その地点から即プレイ・落下してもそこから再開）
5. **保存** → Ctrl+S（ヒエラルキーの「自動保存」をONにしておくと安心）

---

## 1.5 レールの形はゲームビューで直接いじれる

- **線にマウスを乗せる**と緑の「＋」が出る → **そのまま押して引っ張ると、その場所に点が増えて付いてくる**（坂・山・カーブが1ドラッグで作れる）
- 動かさずにクリック＝従来どおり路線まるごと選択（ギズモ移動）
- ノードを右クリック＝削除 / Ctrl+線クリック＝点を追加だけ（引っ張らない）
- 矢印キー=選択ノードをグリッド移動（Q/E=上下、Shift=1/10微調整）、Delete=削除

## 2. 2D形状エディタ（作成タブ / どのレールでも使える）

レールを「図」で編集する画面。ゲームビューと**常に同期**します。

- **点をドラッグ** = 移動（座標がツールチップ表示。スナップ設定も効く）
- **線を押してそのまま引っ張る** = その場所に点を追加してそのまま動かせる（緑の「＋」が目印）
- **右クリック** = 点を削除
- **ドラッグ中 Shift = 縦（高さ）だけ / Ctrl = 横だけ**（まっすぐ動かす軸制限。ゲームビューのハンドルでも同じ）
- **ホイール** = カーソル位置を中心にズーム / **中ボタンドラッグ** = スクロール / 「全体表示」で戻る
- 触った点は下の **「ノードN: X Y Z」** 欄で数値でも合わせられる（正確な高さ・位置決め用）
- **断面コンボ**で見る面を切替：
  - **横見図（X-Y / Z-Y）** … 高さの編集。坂・山なり・リフトの縦移動
  - **上から（X-Z）** … 奥行き⇔横向きの**平面カーブ**。「奥へ走る道から横スクロールへ滑らかに曲がる」はここで作る
- スプライン路線は**実際に通る曲線**がそのまま表示される（薄い直線=点の並び）

> コツ：大まかな形は「形から生成」で置き、細部は2Dエディタで整えるのが速い。

---

## 3. 動くレール・リフトのレシピ集（動きタブ）

対象のレールを選んで「動き (Motion)」タブへ。**「動きをプレビュー」をON**にすると編集中も実際に動く（ゲームビュー右クリックメニューからも切替可）。

### レシピA：ゆれる足場（一番簡単）
- 振幅 XYZ に値を入れるだけ（例: `0, 2, 0` 周期3秒 = 上下±2mを3秒で往復）
- 波形「停止つき往復」にすると端で一瞬止まって乗りやすい

### レシピB：ヨッシー1-1の列車（縦⇔横を滑らかに走るリフト）
1. 足場になるレールを選ぶ → 波形 = **「ガイドレール追従」**
2. **「新規作成」** を押す（縦6mのガイドが自動で付く）
3. 経路を描く——**ゲームビューに出るオレンジの点をそのままドラッグ**でOK
   （点をつかんでも選択は切り替わらない / Ctrl+クリック=線上に点を追加 / 右クリック=点を削除。
    数値で正確に合わせたい時はタブ内の**リフト経路エディタ**（2D図）でも同じ操作ができる）
4. 動き方を選ぶ：
   - **「往復」** … 端で緩急つきの滑らかな折り返し（行ったり来たりのリフト）
   - **「片道（到着したら止まる）」** … 目的地に着いたら停車。**乗ったら動き出すと組み合わせると
     「乗る→出発→到着して停車」の本物の駅⇔駅の列車**になる（周期=片道の秒数）
   - 「一周ループ」 … 閉じたガイドをぐるぐる回り続ける
5. **「列車式：向きも経路に合わせて回す」をON** — 足場がガイドのカーブに沿って
   **向きを変えながら転回**する（奥向きの道⇔横向きの道へ、乗せたまま曲がる）
6. **動き出し = 「乗ったら動き出す」** にすると、プレイヤーが乗るまで待機 → 乗った瞬間に出発（本物の列車！）
7. **停車時間 (秒)** で乗り降りの間を調整：
   - 往復 … 両端に着くたびこの秒数だけ停まってから折り返す（飛び乗りやすくなる）
   - 片道 … 出発までの待ち時間（「乗ったら動き出す」なら乗ってから発車までの間）
- 落下でミスすると動くレールは初期位置へ戻る（片道の列車も駅に戻って何度でもやり直せる）
- 区間（緑◆と橙◆をドラッグ）でガイドの一部だけを動くようにもできる
- 周期 = 1往復にかかる秒数
- 列車式OFF = 向きを保ったまま平行移動（真上に運ぶだけのエレベーター向け）
- 奥→横へ曲げるガイドは、経路エディタの断面を**「上から (X-Z)」**にして描くとやりやすい

### レシピC：数珠つなぎの循環リフト（観覧車・エスカレーター式）
1. ガイドにしたいレールを選ぶ（輪っかやらせん状に描く）
2. 動きタブの **「このレールを親にしてリフト足場を一括生成」**（足場数と1周秒数を指定）
- 位相が等間隔で自動設定され、順繰りに回り続ける

### 動き出しの使い分け
- **最初から動く** … 常時動く障害物・振り子など
- **乗ったら動き出す** … 乗ってから出発する列車・エレベーター（一度動いたら動き続ける）
- 待機中のリフトにはプレイ中、金色の**「！」マーク**が浮かぶ（乗れば動くと分かる）

### レシピD：後から出現する道（ヨッシー式の隠し橋）
1. 橋にしたいレールを描く（他の道と溶接しない独立レール推奨）
2. そのレールを選んで動きタブ → **「出現する道（後から現れる）」** → チェックON
3. **発動レール**（ここに乗ると出現する）を一覧から選ぶ
4. テストプレイ → 発動レールに乗った瞬間、橋が**下からせり上がって出現**する
- プレイ開始時は見えない・乗れない・着地もできない（すり抜けて落ちる）
- 道の種類を「SDF溶け道」にすると、とろけながら出現する演出になる
- エディタ中は**紫の線**で表示され、発動レールとの間にリンク線が出る

---

## 4. ゲームビューでの直接操作（パネルを開かずに作業）

| 操作 | 効果 |
|---|---|
| **右クリック** | メニュー：ここからテストプレイ / 敵・コイン・ブロックを配置 / 近くの配置物の削除・種類切替 / 動きプレビュー切替 |
| **敵をドラッグ** | つかんで移動（別レールへの乗せ替えも可） |
| **コインをドラッグ** | 移動。**Shift+上下ドラッグ = 高さ調整** |
| **ブロックをドラッグ** | 移動（上下=段、左右=横ずれ）。**Ctrl+ドラッグ = 複製** |
| ブロック配置モード中 | クリック=置く / 右クリック=消す（配置エディタでON） |

---

## 5. 確認・デバッグ

- **ここからテストプレイ**（右クリック）… 作った場所だけを何度も試す。Editに戻ると通常スタートに復帰
- **動きをプレビュー** … Playせずにリフトの動きを確認（「乗ったら動く」も強制的に動く）
- **ミニマップ**（マップアイコン）… コース全体を俯瞰。クリックでカメラ移動
- **検証（レールエディタ）** … 到達チェック＆経路表示 / ジャンプ予測線
- **インスペクター（詳細設定）** … トップビュー切替・ポストエフェクト調整

---

## 6. 困った時

| 症状 | 対処 |
|---|---|
| クリックしても何も置けない | ブロック配置モードが残っていないか確認（配置エディタ）。パネルを閉じれば自動で解除される |
| ブロックがずれて見える | 「道を再生成」ボタン（インスペクター）で作り直し |
| ドックの配置がバラバラ | メニューバー「ウィンドウ」→「レイアウトをリセット」 |
| リフトの経路がずれた | レール編集でガイドが変形していないか2D形状エディタで確認 |
| 保存し忘れが怖い | ヒエラルキーの「自動保存」をON |
